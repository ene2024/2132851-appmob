import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'temporary-2132851',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
